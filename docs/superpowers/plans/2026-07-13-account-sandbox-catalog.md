# Account Sandbox Catalog Implementation Plan

Implementation record for the account- and organization-scoped Sandbox catalogs completed on this branch.

**Goal:** Add account- and organization-scoped pages for region-filtered sandbox templates and region/template-filtered runner instances.

**Architecture:** Add user APIs backed by the selected account or GitHub installation Sandbox credentials, with an allowlisted region-to-endpoint mapping. Expose templates and runner-created instances as peer account-setting tabs.

**Tech Stack:** Go `net/http`, Qiniu Go SDK sandbox client, React, TypeScript, Tailwind/shadcn.

## Global Constraints

- Keep APIs user-scoped and never return Sandbox credentials.
- Edit `ui/`; do not hand-edit generated `internal/server/ui/` assets.
- Support `cn-yangzhou-1` and `us-south-1` consistently with account preferences.

---

### Task 1: Sandbox catalog API

**Files:**
- Modify: `internal/server/server.go`
- Create: `internal/server/server_sandbox_catalog.go`
- Test: `internal/server/server_helpers_test.go`

**Interfaces:**
- Produces: `GET /user/sandbox/templates?region=<id>` and `GET /user/sandbox/instances?region=<id>&template_id=<id>`.

- [x] Write failing tests for region validation and catalog responses.
- [x] Run focused Go tests and confirm the expected failures.
- [x] Implement authenticated account credential lookup, region endpoint selection, SDK listing, and response projection.
- [x] Run focused Go tests and confirm they pass.

### Task 2: Account catalog pages

**Files:**
- Modify: `ui/src/admin-types.ts`
- Modify: `ui/src/components/user-dashboard.tsx`
- Create: `ui/src/components/sandbox-catalog-sections.tsx`
- Modify: `ui/src/App.tsx`

**Interfaces:**
- Consumes: the two catalog endpoints from Task 1.
- Produces: account and organization Sandbox Templates and Sandbox Instances routes.

- [x] Add types and route/navigation declarations.
- [x] Add focused list sections with loading, error, empty, filtering, and refresh states.
- [x] Wire account/organization routes in `App.tsx`.
- [x] Run UI lint/build and fix all failures.

### Task 3: Integrated verification

**Files:**
- Generated by command only: `internal/server/ui/`

- [x] Run `go test ./internal/server -count=1`.
- [x] Run `task build`.
- [x] Open account and organization Sandbox routes in the real app and verify both filters.
- [x] Run `git diff --check` and inspect the final diff.
